# Contributing

感谢你改进 LaunchScope。

## 开始开发

1. 使用 macOS 14+ 和 Swift 6 工具链。
2. 创建分支并保持改动聚焦。
3. 运行 `swift test`。
4. 运行 `./scripts/build-app.sh`，检查生成的应用可以启动。

## 识别规则

只有用途明确、能从公开产品文档或稳定安装路径验证的规则才适合加入内置目录。请同时提供：

- 稳定且尽量窄的匹配字符串；
- 面向普通用户的用途说明；
- 停用后可能影响的功能；
- 对应的单元测试。

个人项目、公司内部工具和包含用户名的规则应放在本机 `purpose-rules.json`，不要提交到内置目录。

## 代码要求

- 扫描逻辑保持只读；写操作集中在 `LaunchdManager`。
- 不静默吞掉 `launchctl` 或 AppleScript 错误。
- 操作成功必须通过重新读取系统状态验证。
- 不新增网络、遥测或 AI 依赖，除非经过单独的公开设计讨论并默认关闭。
- UI 文案要区分事实、推断和未知。

提交 Pull Request 时请说明行为变化、风险、测试命令和人工验收范围。不要附带包含用户名、完整命令参数或私人 Label 的扫描结果。
